# `raw.txt` 41 项决策落实表

> 状态快照：2026-08-31，依据当前工作区代码（R0–R4 已完成）判断。
>
> 本表把 [`raw.txt`](raw.txt) 中编号 1–41 的讨论整理为可执行决策。发生冲突时遵循：**较晚的明确确认覆盖较早草案；[`collaboration-target-architecture.md`](collaboration-target-architecture.md) 中已经冻结的最终语义覆盖讨论过程；不为被覆盖的方案保留兼容层。**

状态只表示代码落实程度，不表示讨论是否已经确认：

- `完成`：最终语义已经落到当前代码，并有相称的自动化验证；是否提交 Git 不影响该判断。
- `进行中`：已有一部分能力或基础设施，但仍保留旧模型、旧协议或缺少关键闭环。
- `未开始`：最终行为尚未进入产品代码；仅有文档和设计不算开始。

当前汇总：`完成 24`、`进行中 16`、`未开始 1`。

R4 已完成 desired state、持久 home/临时 runtime 分层、配置热更新与严格会话失效、archive/restore、current-session Engine readiness、Desktop SSE 状态投影及 60 秒全量对账。仍未完成的是按约定暂缓的 Desktop supervisor 故障/退出 E2E、R5 的消息/Climate 闭环、R6 的 Board/Agenda 闭环，以及部分跨连接和并发行为验收；这些条目不会因为 R4 完成而提前标记为 `完成`。

| # | 最终决策：这一项做什么 | 如何实现与验收 | 状态 | 前后矛盾的最终裁决 |
|---:|---|---|---|---|
| 1 | Collaboration Runtime 的生命周期与 Desktop 正常生命周期绑定，但 Runtime 仍由 **Server 子进程 + Computer daemon 子进程**组成；Desktop 是唯一 supervisor，退出时全部停止。 | R3 已删除 launchd 和 `control.sock`；Desktop 生成 `runtime_session_id`，启动并持有两个子进程，等待随机 loopback HTTP ready；正常退出按 Computer/Engine → Server 顺序关闭；子进程故障时废弃旧 session 并成组重启。尚缺真实 Desktop 子进程正常退出及 Server/Computer crash 成组重启 E2E。 | 进行中 | 本条早期草案中“把 Runner 直接放入 Tauri，并删除 Computer、HTTP seam、inventory、engine_id”已被第 8–10、22、27、40–41 项及后续明确确认推翻。只保留“Desktop 拥有生命周期”，不采用单进程合并。 |
| 2 | 同时保留两种不同关系：Room Membership 是客观可见范围；Climate 是某 Agent 对另一 Participant 的私有、方向性、跨 Room 印象。 | Membership 继续由 Room 模块维护；R3/C1 已建立按 `(agent_id, about_participant_id)` 唯一的 Climate 当前状态表；R5 仍需增加所有者读写、Turn 投影以及 A→B、B→A 独立测试。 | 进行中 | `raw.txt` 早期提到系统按 mention/reaction 自动微调 Climate；第 4、5 项及最终架构改为 **只允许 Agent 显式更新**，且 reaction 已在 R3 完全删除。 |
| 3 | Climate 只保存当前的 `affinity`、`trust`、`last_note`、`updated_at`，不绑定 Room、不要求对称。 | R3/C1 已建立 `collab_agent_climates`，两个 Participant 外键带数值范围、自关联禁止和复合主键约束；R5 仍需增加所有者读写与方向独立测试。 | 进行中 | 本条原始模型里的 `history` 被后续“Climate 不保留历史”决定删除；当前 schema 没有 history JSON、事件表或回放机制。 |
| 4 | Climate 数值和 note 只由所属 Agent 根据实际交流显式修改，系统不在后台暗改。 | R5 只通过结构化 `climate show/note` 命令更新；Server 从 Agent JWT 确定 `agent_id`，禁止替其他 Agent 写 Climate；消息、mention、HELD 和 reaction 都不触发隐式增减。 | 未开始 | 本条在 `raw.txt` 中是问句，后续会话及目标架构已经回答为“是”；与第 2 项早期的自动微调描述冲突时，以显式更新为准。 |
| 5 | Group 的创建和成员变更只属于 Desktop 用户；Agent 不能创建、邀请、踢人或退群，也不提供 reaction/emoji。Agent 只能向现有 Group 发言，但可以按第 36/38 项创建或复用唯一 Direct Room。 | R3 已将旧 CLI 切为 typed `AgentCommand`，其 enum 不包含 Group 管理或 reaction；reaction 表和旧命令均已删除，Group 创建与成员变更只存在于 Desktop command。architecture/cutover 测试约束旧入口不能恢复。 | 完成 | “Agent 只能在既有房间交流”被后续 Direct Room 决定收窄为“不能改变 **Group** 受众”。Direct Room 是固定两人、唯一键约束的会话，不是 Agent 管理成员关系。 |
| 6 | User 消息确定性进入正式 Turn，不经过小模型拦截；Agent 消息才经过 triage，以抑制重复回复和 Agent 循环。 | 当前 Server 先按作者 kind 和消息类型做确定性判定；仅 Agent-only 场景调用 classifier。新的 fake OpenCode daemon E2E 覆盖用户消息进入正式 Run 并得到一次回复，triage parser 测试继续覆盖模型结果边界。 | 完成 | 无冲突；“可能同时唤醒多个 Agent、产生多个正式调用”是已接受的产品代价。 |
| 7 | 保留 seen/HELD 新鲜度检查：Agent 可以并行思考，但发布回复前必须基于最新 Room sequence；过时回复只能 glance 后使用一次性 token 重试。 | seen/HELD 已迁入 typed `AgentCommands`，Redis binding、glance anchor和一次性 token 逻辑仍在；但旧 P2 并发/stale reply 测试随旧协议删除后尚未用新 `/agent/commands` 接口补回。 | 进行中 | HELD 不是全局锁，也不选举唯一回答者；它只约束发布时的新鲜度。当前代码路径存在，但在新协议下仍缺相称的并发回归验收。 |
| 8 | 保留 PostgreSQL 与 Redis 作为本机基础设施，产品仍限定单台 Mac、单 Desktop 生命周期、单逻辑 Runtime、本机 Engine；Desktop 正常退出后协作进程停止，但 PostgreSQL/Redis 进程继续运行。 | Redis 继续承载 Pub/Sub、seen、HELD、cooldown、dedupe；R3 已用 Desktop supervisor 和 RuntimeSession 删除远程 Computer/launchd 生命周期，关闭逻辑不管理 PostgreSQL/Redis 进程。尚缺真实 Desktop 正常退出后的进程审计 E2E。 | 进行中 | “保留 Redis”不意味着保留远程 Mac、多 Computer、后台 daemon 或让 Desktop 管理 Redis 进程。当前源码已无 launchd 和持久 Computer registry；未完成的是产品级退出验收，不是架构切换代码。 |
| 9 | Desktop 监督独立的 Collaboration Server 与 Local Computer；Server 管业务事实和认证，Computer 管 Runner、home 和 Engine 子进程，两者只经 HTTP/SSE protocol 通信。 | R3 已用 Desktop 直接 spawn 替换 launchd并使用随机 loopback listener；Computer 子进程显式移除数据库/Redis环境变量，Server 不启动 OpenCode；architecture test 约束 Computer 无 SQLx/Redis、Server 无 Engine process spawn。 | 完成 | 与第 1 项的“删除独立 Computer”冲突时，以本条及用户后续“保留独立 Computer daemon，由 Desktop 监督”为准。当前保留的是 Desktop 监督的独立进程，不是 launchd 常驻设备。 |
| 10 | 每次 Desktop 启动生成临时 Desktop secret、Computer secret 和 `runtime_session_id`；Computer 只能为该 session 的 active Agent 获取短期 JWT，shim 使用 Agent JWT。任一子进程故障都使整组 credential 失效。 | R3/C2 已在 Server 内存建立 RuntimeSession；按 `/desktop/*`、`/computer/*`、`/agent/*` 分 scope；30 分钟 Agent JWT 携带 agent、session、expiry；generation和持久 device token 已删除。cutover 测试验证 scope 隔离、归档拒绝和旧 session JWT 失效，尚缺真实子进程 crash 后整组 credential 轮换 E2E。 | 进行中 | 临时 RuntimeSession 已替代旧 device token/generation 链；保留“进行中”只因为 supervisor crash 验收未完成，不是因为仍存在旧身份模型。 |
| 11 | 保留真实多 Engine seam，但当前生产只注册 OpenCode：强类型 `EngineId`、registry、通用 adapter、per-Agent runtime 和 fake adapter；不实现 Codex 空壳、fallback、跨 Engine session 迁移或 capability matrix。 | R2 已在 `computer::engine` 引入 `EngineRegistry`、`EngineAdapter`、`AgentEngineRuntime` 与 fake adapter；OpenCode 原生 CLI、JSONL、session 细节封装在 adapter/runtime 内。接入 Codex 时才新增真实 adapter。 | 完成 | 本条早期单层 `AgentEngine::run_turn` 已被第 33 项的两层接口覆盖；本条“暂不保留 inventory 表”又被第 13–14 项覆盖，最终仍持久化 last observation。 |
| 12 | `engine_id` 是每个 Agent 的真实 RuntimeConfig 属性；Runner 创建时按它选 adapter，Engine/model/persona 变化时重建 Runner，旧 Engine session 不跨 Engine 恢复。 | `RunnerFactory` 使用 `EngineRegistry::require(engine_id)` 创建独立 runtime；assignment 变化会停止旧 Runner。OpenCode 仅在 Engine、主模型和最终受管 `AGENTS.md` 的 context fingerprint 全部一致时恢复 session，任一不一致或旧 `persona_hash` 元数据都会清除；生产 registry 当前仅有 OpenCode。 | 完成 | 示例中的 `codex → CodexAdapter` 只表达未来可扩展性，不代表现在注册 Codex；与第 11/33 项一致，当前禁止占位实现。 |
| 13 | Engine inventory 持久化到 PostgreSQL，但只表示最后一次探测结果。 | R3/C1 已将旧 `collab_computer_engines` 替换为不依赖 Computer 的 `collab_engine_inventory`；Computer probe 后上报 status/version/error/checked_at，Server 写入当前 `observed_session_id`，后续观测覆盖同一 Engine 的旧行。 | 完成 | 第 11 项早期说“不保留 inventory 表”，本条及第 14 项是更晚明确决定，覆盖前者。当前 inventory 已不再绑定 `computer_id`，只是一行 last-known observation。 |
| 14 | 数据库 inventory 是 last-known observation；当前 Runtime 能否启动 Runner，必须同时由当前 session 的实时 probe 确认，不使用过期 `ready`。 | inventory 行保存 `observed_session_id`；Computer 启动和周期 rescan 直接 probe adapter，只在当前内存 readiness 为 `ready` 时 reconcile Runner。R4 E2E 先植入旧 session 的持久化 `ready`，验证它不会产生 current readiness 或启动 Runner，再验证当前 Computer probe 后才形成实时 `ready` 和 Runner。 | 完成 | “持久化”不等于“权威可用性”：PostgreSQL 行只供展示最后观测，当前 session 的 heartbeat readiness 才能决定是否运行。 |
| 15 | 每个 Agent 显式保存 `engine_id`、`main_model_id`、`triage_model_id`；任何 Engine、模型或 persona 改变都重建 Runner，并防止恢复不兼容 session。 | R4 已提供 Desktop `UpdateAgent` 完整命令和编辑界面；更新会原子递增 `config_revision`、经 management SSE 触发 Runner 重建。E2E 覆盖 role/persona/main/triage model 热更新与旧 session 清除；单元测试分别覆盖 Engine、主模型、完整 context fingerprint 不兼容。 | 完成 | 当前生产只注册 OpenCode，因此 UI 固定 `engine_id=opencode`；协议和 Server 更新边界仍保留真实 `engine_id`，未来只有新增可用 adapter 后才增加选择项。 |
| 16 | 每个 Agent 使用裁剪版持久 home：受管 persona、私有 `work/`、Engine continuity state；不创建 Memory、Notes、Skills，也不共享真实项目目录。临时 shim/JWT/派生配置不放进持久 home。 | R4 已落地 `~/.openwork/agents/<id>/{AGENTS.md,work/,engines/<engine>/session.json}`；共享 shim、每 Agent runtime token、Engine cache/state 和派生配置全部位于 `~/.openwork/runtime/<session-id>/`。架构、home 单元和真实 R4 E2E 均验证持久 home 不生成 `bin/memory/notes/skills/workspace`。 | 完成 | 本条早期的外置 `engine-state/` 和旧 `computer/agents` 布局均已删除；最终连续性状态固定在 Agent home 的 per-Engine 目录中。 |
| 17 | Board、Column、Card 继续作为共享任务事实，不因为裁剪 Cumora 功能而删除。 | 三类实体继续由 Server/PostgreSQL 持久化，Desktop 展示 Board，typed AgentCommand 可在授权范围内读取和推进 Card；R3 cutover 测试验证新 Board 原子创建三列并标记一个 terminal Column。 | 完成 | R3 已同时完成 Room 解耦和 claim 双状态删除；这不代表 R6 的完整 Board/Column 管理、删除规则和并发排序验收已经完成。 |
| 18 | Desktop 用户定义 Board/Column 工作流；Agent 读取结构并可 create/claim/assign/update/move Card，但不能创建、删除、重排 Column，也不能删除 Board/Column/Card。 | typed `AgentCommand` 已只暴露 Board/Card 白名单，create/claim/move 走 Server 事务、JWT actor、active Run 与幂等校验；R6 仍需 Desktop Column 管理、受约束删除及 Agent show/assign/update。 | 进行中 | raw argv 和 Room Board 已在 R3 删除；当前未完成的是完整命令面和 Desktop 结构管理，不再是旧协议残留。 |
| 19 | Agenda 默认关闭、按 Agent 开启，只在当前 Desktop Runtime 在线时运行；候选来自 assigned 且非 terminal 的 Card和停滞 Room，先经 triage，Redis 保存 cooldown/dedupe，不增加 Calendar、提醒或离线补跑。 | R3 已切为 `agenda_enabled`、workspace Card、显式 `is_terminal` 和 session-fenced Run；Computer 只随当前 Desktop Runtime 运行。候选、triage、Redis协调代码已迁移，但新的 Card/Room Agenda 联合测试和退出后不补跑验收仍属 R6。 | 进行中 | `scanner_enabled`、Room绑定 Board和 launchd 常驻均已删除；保留“进行中”是因为 R6 的完整行为及并发验收尚未补齐。 |
| 20 | Agent 数据拆成 `Participant`、`AgentProfile`、`AgentRuntimeConfig`；代码拥有不可被 persona 覆盖的 `CollaborationContract`。删除含义重叠的 `bio`、`system_prompt` 和宽 Agent 表。 | 三层 schema 和字段切换已完成；R4 `HomeManager` 始终用身份、用户 persona 与代码拥有的 Collaboration Contract 生成完整 `AGENTS.md`，并以最终文件内容计算 context fingerprint。Persona 只能替换自己的字段，不能移除代码附加的协作契约。 | 完成 | 旧 `collab_agents.bio/system_prompt` 已删除；“删除 system_prompt”不是删除 Agent 个性，而是把用户可编辑内容统一为 `persona`，系统协作规则继续由代码拥有。 |
| 21 | Collaboration Server 是 Agent desired config 的唯一事实来源；Computer 只同步、reconcile 并上报实际 Runtime 状态。startup snapshot、management SSE 和 60 秒 full snapshot fallback 进入同一个幂等 reconcile。 | R4 已把 startup snapshot、management SSE 和周期 full snapshot 汇入同一 reconcile。真实 PostgreSQL/Redis E2E 覆盖：单 Agent home 失败不阻塞其他 Agent、修复后恢复、SSE 配置更新、故意绕过 SSE 的数据库变更由 full snapshot 找回、新建/归档/恢复均不重启 Runtime。 | 完成 | Computer 不保存第二份 desired config；SSE 只要求“重新取完整快照”，即使事件丢失也由周期对账收敛。 |
| 22 | PostgreSQL 保存 Agent/Profile/RuntimeConfig、inventory observation 与 RunHistory；Server 内存只保存当前 RuntimeSession 和 Runner/Engine 投影。删除持久 Computer 设备模型、generation、pair/repair/revoke。 | R3/C1–C2 已用随机 `runtime_session_id` fencing重写 schema与认证；Run保存 session、Engine和模型快照但不外键到 Computer，RuntimeSession和JWT signing key只在内存中；旧 Computer表、generation和device token均已删除并由cutover测试审计。 | 完成 | 本条明确覆盖第1项“连inventory、engine_id一并删除”的过度裁剪；实际删除的是 **持久设备身份**，独立Computer进程、Engine配置和历史observation均保留。 |
| 23 | 状态所有权固定为：PostgreSQL=持久业务事实，Agent home=Engine/工作连续性，Server 内存=当前 RuntimeSession，Redis=短期协调；任何层都不能成为另一层的隐式备份。 | PostgreSQL/RuntimeSession/Redis 边界已在 R3 完成；R4 又把工作文件与 Engine session 留在持久 Agent home，把 runtime token、shim、cache/state 和派生配置移入当前 session 的 runtime 目录，并让 Server 内存仅接收 Computer heartbeat 的 current readiness/Runner 投影。 | 完成 | `collab_computers`、generation、持久 device token 和旧混合 home 均已删除；各层现在只保存自己负责的状态。 |
| 24 | RoomMember、MessageAuthor、CardAssignee 统一引用 Participant；Agent 专属 Profile/RuntimeConfig 分表。本机用户固定为不可修改、不可归档的 `local-user`；Agent ID 由 Server 根据 display name 自动生成并永久不变。 | R3/C1已建立统一Participant外键、三层Agent数据和不可变`local-user`；Agent slug使用ASCII规范化、24字符基础、必要时`a-`/`agent` fallback和安全四字符冲突后缀，Desktop不再提交ID。slug、local-user trigger和cutover流程已有测试。 | 完成 | 纯中文display name使用`agent`基础，不引入拼音依赖；旧`user`身份、宽Agent表和Desktop自填ID路径均已删除。 |
| 25 | Agent 生命周期只有 active/archived：产品“删除”写 `archived_at`，停止 Runner/JWT/wake/Agenda但保留消息、Run、Card、Climate、home 和 session；物理删除 home 是独立破坏性操作。 | active/archived 已成为唯一生命周期；desired snapshot、JWT、wake 和 Agenda 统一过滤 active。R4 E2E 验证 archive 后真实 Computer 移除 Runner、持久 `work/` 内容不变，restore 后同一 Agent 按原 `config_revision` 重新进入 running。 | 完成 | 不照搬 Cumora 的 `departed_at` 字段名，也不保留并行生命周期术语；归档不会触发物理 home 删除。 |
| 26 | 不拆 crate，继续使用单一 `openwork-collab`，与 Desktop 同步构建和升级。 | Server、Computer、protocol、shim 和 Engine 保持同 crate 内模块；通过模块边界而不是 crate 数量表达隔离。 | 完成 | 无冲突；“模块化”不等于“拆 crate”。 |
| 27 | 单 crate 内保持 Server/Computer 双门面：二者只经 protocol HTTP/SSE DTO 通信；业务模块默认私有；Engine seam 属于 Computer；不增加只转发的 Repository/Service/Manager；最终删除 launchd。 | R1已深化Server，R2已深化Engine；R3已把共享线协议按desktop/computer/agent拆分，删除launchd/control socket/runtime旧门面，主要入口收敛为CollaborationServer、ComputerDaemon和typed command/result。architecture test持续约束跨层依赖。 | 完成 | 与第1项的in-process合并冲突时采用双门面；当前源码已无launchd、旧protocol单文件或万能Repository/Service。 |
| 28 | 删除“大一统 Storage”，由 agents、rooms/messages、runs、board、coordination、agenda 等业务模块直接拥有 SQL、事务、领域错误和测试；db 只提供 pool/migration/事务原语。 | R1 已删除 `server/storage.rs` 与 `CollaborationStore`；业务 SQL 分入 owning modules，并由 architecture test 防止 control/runtime/scheduler 重新吸收业务 SQL或换名成万能层。 | 完成 | 不为“可替换数据库”预先创建 Repository 接口；当前只有 PostgreSQL，浅转发层没有价值。 |
| 29 | 确认 R1 持久化边界，同时确定 Runner actor 模型：同 Agent 串行、跨 Agent 并行；busy wake 合并为一次 rerun，消息正文始终从 durable inbox 重读，不在内存排无限队列。 | 当前每个AgentRunner独立运行，正式Turn不重入；有界wake channel合并提示，Turn后重新读取durable inbox。旧P2/P3并发/settlement测试已随旧协议删除，新typed Runtime下的busy-wake coalescing和跨Agent并发测试尚未补回。 | 进行中 | 本编号同时包含上一项确认和下一个Runner决定，两部分都保留；实现仍在，但按本文完成口径，缺少新协议回归测试不能继续标“完成”。 |
| 30 | Computer 持有互相独立的 main 和 triage 资源池，默认 `main=2`、`triage=4`，并使用共享 pacer；Adapter 把 provider 错误转换为结构化 `RateLimited { retry_after }`。 | R2 将 scheduler 命名改为 Engine-neutral `local()`，保留两个 semaphore；Runner/Scheduler 只匹配结构化错误；OpenCode adapter 同时归一化 JSONL 和 stderr 限流；测试覆盖 429、取消和输出上限。 | 完成 | 字符串识别没有完全消失，而是被严格限制在 OpenCode adapter 的 provider 翻译边界；Runner/Scheduler 不再猜错误文本。 |
| 31 | 跨 Agent 受 2/4 资源池并发；Desktop 正常退出时还必须执行有界优雅关闭：不接新 wake，最多等 15 秒，随后取消 Engine，保存可保存的 session，并让未完成 Run 保持可恢复。 | 2/4资源池继续由R2提供；R3已实现Runner共享15秒deadline、Computer→Server关闭顺序、SIGTERM后有界强制终止、Engine process-group cancellation，以及新session启动时把旧running Run标记interrupted。尚缺真实Desktop退出和超时Engine验收。 | 进行中 | 不能把“15秒收尾”理解成允许后台继续运行；launchd已经删除，当前剩余的是关闭行为E2E而不是实现入口。 |
| 32 | 确认 15 秒退出流程；Redis Pub/Sub 与 SSE 只传可丢失、可重复的 invalidation hint，不传完整消息或配置。消息靠约 20 秒 inbox poll补偿，配置靠 60 秒 full reconcile补偿。 | Desktop、management 和 per-Agent SSE 均只发送 invalidation；Desktop WebView 已在事件后重新取 canonical snapshot。R4 E2E 通过绕过事件直接改数据库，验证配置仍由 full snapshot 收敛；20 秒 inbox poll 继续保底。仍缺 R5 消息侧丢事件及跨连接断线恢复的联合验收。 | 进行中 | Redis/SSE 始终不是业务事实来源；R4 已完成配置与 UI 投影，剩余缺口属于消息语义和完整连接故障测试。 |
| 33 | 将单泛型 adapter 正式替换为 `EngineRegistry + EngineAdapter + 每 Agent AgentEngineRuntime`；Runtime 隐藏 OpenCode `--session` 和未来 Codex app-server thread。 | R2 已完成：ComputerDaemon 不再泛型；RunnerFactory 按 assignment engine 选 adapter；OpenCode runtime拥有 session load/save/reset；EngineError 通用化；fake adapter 验证成功、限流、session invalid、取消、输出超限和两个 Agent 独立状态。 | 完成 | 这是第 11 项较粗单层草案的最终深化版本。没有 `prepare_home` 独立方法并非缺失：当前必要的 OpenCode 派生配置由 adapter 的 runtime/classify 边界拥有，未来只有出现真实复用需求才扩展。 |
| 34 | Board 是整个本地 workspace 的共享模块，与 Room 平级；删除 `boards.room_id`，不增加只有一个合法值的固定 `workspace_id`。Card-focused Agenda Run 可以没有 `room_id`。 | R3/C1已删除`boards.room_id`且未引入固定`workspace_id`；Board查询、Desktop创建、Card候选和Run都已接受无Room形状，Card可分配active Participant。仍缺card-focused Agenda无Room的专门E2E和R6完整权限验收。 | 进行中 | workspace级Board已落地；“workspace级”仍只表示共享业务模块，不表示多个Agent共享真实本地项目目录。 |
| 35 | `card claim` 不是第二套锁状态，而是“当 `assignee_id IS NULL` 时原子 self-assign”；删除 `claimed_by/claimed_at`，重复 claim 自己幂等，claim 别人的 Card 返回冲突。 | R3/C1已删除`claimed_by/claimed_at`；`claim_card_in`在事务内锁Card，仅允许未分配时self-assign，对自己重复claim幂等，对他人已分配返回冲突，且terminal Column不可claim。仍缺两个Agent并发claim只有一个成功的数据库测试。 | 进行中 | 旧双状态已经删除；由于明确要求的并发胜者验收尚未补回，本条暂不标完成。 |
| 36 | 再次确认第 35 项 claim 语义；同时允许 Agent 原子创建或复用两 Participant 唯一 Direct Room，但继续禁止管理 Group。 | Claim已按第35项落地；Direct Room使用排序participant pair的唯一`direct_key`，数据库唯一索引处理并发创建，Agent DM禁止self-DM和archived目标，AgentCommand不包含Group管理。仍缺并发首次DM与并发claim测试。 | 进行中 | 本编号在`raw.txt`中同时承接claim确认和Direct Room决定；两部分代码均已进入新协议，剩余是并发验收而非旧模型清理。 |
| 37 | 固定根目录为小写 `~/.openwork`，严格区分持久 `agents/<id>/` 与临时 `runtime/<runtime-session-id>/`；runtime 保存共享 shim、每 Agent token 和派生配置，可整目录清理。 | Desktop 以 `~/.openwork` 为唯一根；R4 将持久 Agent home 固定为 `agents/<id>/`，当前 session 的 shim/token/derived config 固定为 `runtime/<session-id>/`。启动会先安全清理陈旧 runtime，正常停止清理当前 runtime，并用所有权锁阻止两个 Desktop 同时管理该目录。 | 完成 | 旧 `computer/agents`、`computer/sessions` 和持久 home 内的 shim/token 布局已被直接删除，不保留迁移或 fallback。 |
| 38 | 确认 Direct Room 权限，并冻结 Agent CLI 能力：读取 inbox/rooms/messages/members/participants；reply/ack/dm；Board/Card 白名单；Climate show/note。明确没有 reaction、Group 管理、Board/Column 结构管理、Memory、Skills。 | typed AgentCommand 已统一校验 active Agent、current session、active Run、request ID 和 JWT actor；reply/ack/dm 及部分 Board/Card 命令已存在，reaction 和 Group 管理已删除。R4 又删除了 Agent home 中的 Memory/Notes/Skills 目录；R5/R6 仍需补完整读取、Climate 与 Card 命令。 | 进行中 | raw argv、reaction、Group 管理以及 Memory/Notes/Skills 存储都已消失；当前差距只剩目标命令面尚未全部实现。 |
| 39 | 借鉴 Cumora 的“薄 shim + Server 权限裁决”，但不复制 raw argv wire protocol；shim 在本机解析为结构化 `AgentCommand`，Server只接收语义命令并返回 `AgentCommandResult`。 | R3/C3已删除`/runtime/cli { argv }`和整个Server raw parser；protocol定义tagged command/result/effect，shim负责参数、stdin/file边界与文本渲染，Server按typed payload做权限、语义hash和幂等结果重放。cutover和fake OpenCode E2E均经过该链路。 | 完成 | Cumora选择raw argv是为了跨版本、远程daemon和多调用方兼容，OpenWork没有这些约束；当前不保留任何argv fallback。 |
| 40 | Server 使用一个随机端口的 loopback HTTP listener，通过 route namespace和 credential scope区分 Desktop、Computer、Agent；Desktop实时更新也走SSE，不使用 Unix socket或WebSocket。 | Server 绑定随机 loopback 端口，三类 namespace/credential scope、stdin bootstrap、ready stdout 和 scope 隔离测试均已完成。R4 将唯一 Desktop SSE 交给 Tauri 后端，再以 typed invalidation 转发给 WebView；前端收到事件后重新获取 Agent 与 Runtime/Engine/Runner snapshot 并展示实际状态。 | 完成 | 固定端口、Unix socket、自定义 framing 和 WebSocket 均不存在；Desktop 实时链路已经从“保持连接”闭合到 UI snapshot 投影。 |
| 41 | 实时拓扑最终为：1条 Desktop UI SSE、1条 Computer management SSE、每个 active Agent各1条独立 Agent SSE；所有写操作走HTTP，不引入WebSocket。 | 三层 SSE 拓扑和 HTTP 写入已经落地；management 触发 roster reconcile，Agent SSE 只接收当前 JWT 主体的 wake，Desktop SSE 已消费到 WebView，三类连接均有退避并由 60 秒 config snapshot/20 秒 inbox poll 保底。仍缺连接数量与多连接断线恢复的联合测试。 | 进行中 | “每 Agent 独立连接”不能取代 management SSE，因为新 Agent 尚无 Runner/JWT；剩余问题是拓扑级验收，不是协议形状或 UI 消费缺失。 |
